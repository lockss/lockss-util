package org.lockss.util.rest.repo.model;

/**
 * Indicates whether an endpoint should return all versions of artifacts
 * or only the latest versions of artifacts.
 */
public enum ArtifactVersions {
  ALL,
  LATEST
}
